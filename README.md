# Files Storage

This repository is used as a public file storage for government recruitment-related documents such as:

- PYQ (PDF)
- Image of products

Files are uploaded automatically using Python scripts.
Each file generates a stable public URL via raw.githubusercontent.com,
which is then stored in structured JSON and databases.

This repository does NOT contain application source code.
